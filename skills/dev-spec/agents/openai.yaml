interface:
  display_name: "Dev Draft Spec"
  short_description: "Write one decision-ready specification under $PWD/docs/specs"
  default_prompt: "Use $dev-spec to draft a decision-ready specification from the provided investigation report or request plus supporting context, keep confirmed facts separate from proposals and assumptions, and save one spec under $PWD/docs/specs."
