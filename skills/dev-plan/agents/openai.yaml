interface:
  display_name: "Dev Create ExecPlan"
  short_description: "Create, update, or execute one ExecPlan under $PWD/docs/plans"
  default_prompt: "Use $dev-plan to create, update, or execute an ExecPlan for this repository by following references/PLANS.md closely. Treat free-form requests and upstream notes as planning inputs first, and execute only when the user explicitly authorizes plan execution."
