interface:
  display_name: "Dev Build Walkthrough"
  short_description: "Turn a diff or code area into a short prioritized reading path"
  default_prompt: "Use $dev-walkthrough to turn the current changes or a code area into a concise human reading path with a clear start point, ordered steps, watchpoints, and an optional small Mermaid diagram. Save one walkthrough note under $PWD/docs/walkthroughs."
